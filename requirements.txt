# Scraper + pipeline dependencies (Python 3.11+)
aiohttp
requests
Pillow
google-auth
google-api-python-client
google-cloud-storage
# 3.0.5 caps the actor-log stream's read timeout, fixing the crash that killed
# live log forwarding mid-run (impit.TimeoutException in the _stream_log thread).
apify-client==3.0.5
scrapingbee

# Supabase / Postgres access (transaction pooler). psycopg 3.x.
psycopg[binary]>=3.1

# Loads a local .env for development (not needed in CI, where secrets are injected).
python-dotenv
