# Secrets - never commit
.env
.env.*
!.env.example
*service-account*.json
smart-surf-*.json
uplift-*.json

# Data exports (the taxonomy lives in the DB after seeding)
*.csv

# Leaked pre-edit copy of the scraper (still holds old hardcoded keys - delete it)
facebookadscraperapify2026-v2 (1).py

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
env/
*.egg-info/

# Node / Next.js (dashboard, added later)
node_modules/
.next/
.vercel/
dist/
build/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
.vercel
